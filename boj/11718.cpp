#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    while (true) {
        string s; getline(cin, s); cout << s << "\n";
        if (s == "") break;
    }
    return 0;
}
